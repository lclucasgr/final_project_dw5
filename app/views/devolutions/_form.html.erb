<%= render 'layouts/header_librarier' %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
   
    <%= form_for(@devolution) do |f| %>
      <%= f.collection_select(:book_id, @livros, :id, :title)  %> 

      <%= f.submit yield(:button_text), class: "btn btn-primary" %> 
    <% end %>

  </div>
</div>